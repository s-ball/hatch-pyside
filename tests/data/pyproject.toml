[build-system]
requires = ["hatchling", "hatch-pyside @ file:///C:/Users/serge/PycharmProjects/hatch-pyside"]
build-backend = "hatchling.build"

[project]
name="foo"
version = "0.1.0"

[tool.hatch.build.targets.wheel.hooks.pyside]
folders = ["src/foo"]